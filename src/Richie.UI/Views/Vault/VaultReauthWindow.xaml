<ui:FluentWindow x:Class="Richie.UI.Views.Vault.VaultReauthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Confirm master password"
        Height="240" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Confirm master password" />

        <StackPanel Grid.Row="1" Margin="24,8,24,8" VerticalAlignment="Center">
            <ui:TextBlock Appearance="Secondary" TextWrapping="Wrap" Margin="0,0,0,10" Text="{Binding Prompt}" />
            <ui:PasswordBox x:Name="PasswordBox" PlaceholderText="Master password"
                            Password="{Binding MasterPassword, UpdateSourceTrigger=PropertyChanged}"
                            KeyDown="OnPasswordKeyDown" />
            <ui:TextBlock Foreground="#C42B1C" TextWrapping="Wrap" Margin="0,8,0,0" Text="{Binding Error}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <ui:Button Content="Cancel" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <ui:Button Content="Confirm" Appearance="Primary" Command="{Binding ConfirmCommand}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
