<ui:FluentWindow x:Class="Richie.UI.Views.Vault.VaultEntryDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Password details"
        Height="520" Width="460"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding AccountName}" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,8,24,8">
            <StackPanel>
                <ui:TextBlock Appearance="Secondary" Text="Category" />
                <ui:TextBlock Margin="0,0,0,10" Text="{Binding Category, TargetNullValue='—'}" />

                <ui:TextBlock Appearance="Secondary" Text="User ID" />
                <ui:TextBlock Margin="0,0,0,10" Text="{Binding LoginId, TargetNullValue='—'}" />

                <ui:TextBlock Appearance="Secondary" Text="Web URL" />
                <ui:TextBlock Margin="0,0,0,10" TextWrapping="Wrap" Text="{Binding Url, TargetNullValue='—'}" />

                <ui:TextBlock Appearance="Secondary" Text="Password" />
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Grid.Column="0" VerticalAlignment="Center" FontTypography="BodyStrong"
                                  Text="{Binding DisplayPassword}" />
                    <ui:Button Grid.Column="1" Margin="8,0,0,0" Content="{Binding RevealButtonText}"
                               Click="OnReveal" />
                    <ui:Button Grid.Column="2" Margin="8,0,0,0" Content="Copy" Click="OnCopy" />
                </Grid>

                <ui:TextBlock Appearance="Secondary" Text="Notes" />
                <ui:TextBlock Margin="0,0,0,10" TextWrapping="Wrap" Text="{Binding Notes, TargetNullValue='—'}" />

                <ui:InfoBar Severity="Informational" IsOpen="True" IsClosable="False" Margin="0,4,0,0"
                            Message="Copied passwords clear from the clipboard automatically after 30 seconds." />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <ui:Button Content="Open website" Margin="0,0,8,0" Click="OnOpenWebsite"
                       Visibility="{Binding HasUrl, Converter={StaticResource BoolToVisibilityConverter}}" />
            <ui:Button Content="Close" Appearance="Primary" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
