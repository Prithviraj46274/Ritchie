<ui:FluentWindow x:Class="Richie.UI.Views.Expenses.AddIncomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Income"
        Height="460" Width="420"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Title}" />

        <StackPanel Grid.Row="1" Margin="24,8,24,8">
            <ui:TextBlock Text="Date" Margin="0,0,0,4" />
            <DatePicker SelectedDate="{Binding Date}" Margin="0,0,0,10" />
            <ui:TextBox PlaceholderText="Amount" Margin="0,0,0,10"
                        Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}" />
            <ui:TextBox PlaceholderText="Source (e.g. Salary, Freelance)" Margin="0,0,0,10"
                        Text="{Binding Source, UpdateSourceTrigger=PropertyChanged}" />
            <ui:TextBox PlaceholderText="Notes" Margin="0,0,0,10" AcceptsReturn="True" MinLines="2"
                        Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
            <ui:TextBlock Foreground="#C42B1C" TextWrapping="Wrap" Text="{Binding Error}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <ui:Button Content="Cancel" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <ui:Button Content="Save" Appearance="Primary" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
